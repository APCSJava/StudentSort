package student;

public class FirstNameSortStudent extends Student {

	/** Return a negative value if this student's first name occurs alphabetically
	 * before other student's first name.  Return a positive value if this student's
	 * first name occurs after the other student's first name.  In the case that
	 * two students share a common first name, make the decision using their last
	 * names, instead.
	 * 
	 * Hint: String objects already implement compareTo -- leverage their existing
	 * behavior.
	 */
	@Override
	public int compareTo(Student other) {
		// TODO Auto-generated method stub
		return 0;
	}

}
