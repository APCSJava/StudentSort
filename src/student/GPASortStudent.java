package student;

public class GPASortStudent extends Student {

	/** Return -1 if this comes before other by gpa or
	 * 1 if this comes after other.  In the case that the gpa
	 * is exactly the same, use the last name to decide.
	 */
	@Override
	public int compareTo(Student other) {
		// TODO Auto-generated method stub
		return 0;
	}

}
